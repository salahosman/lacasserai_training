<form method="post" action="{{ path('fos_user_delete', {'id': fos_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fos_user.id) }}">
    <button class="btn">Delete</button>
</form>
